# CLAUDE.md - Project Instructions for Claude Code

**READ `AGENTS.md` FIRST** before making any changes to this repository. It is the authoritative source of truth for all design decisions, naming conventions, and rules.

## Critical Rules

- **NEVER run `mdbook build`** - only the user runs builds. Edit `src/` files only.
- **NEVER run git commands** (commit, push, etc.) unless explicitly told to.
- **AGENTS.md wins** over any other file if there is a conflict.
